.DS_Store
.env
.env.local
config.local.js
supabase/.temp/
/secrets/
node_modules/

# Local-only AI memory (contient secrets + contexte privé)
ai-memory/
# Blueprints Make (contiennent clés API)
*.blueprint*.json

# Fichiers de prévisualisation locaux (non publiés)
_preview-*.html

# Outillage local des assistants de code
.claude/

# Documents de travail : contrats, devis… ⚠️ ce dépôt est PUBLIC et servi par
# GitHub Pages — tout fichier commité devient une URL sur replyit.fr.
*.pdf

# Maquettes non publiées
landing-proto.html

# Le backend (Edge Functions + migrations) a déménagé le 01/09/2026 dans le
# dépôt PRIVÉ ReplyIt/replyit-backend, pour ne plus être servi publiquement.
supabase/
